# llm-local (Week 1)

## Setup (Anaconda prompt)
1. Activate env:
2. Install dependencies (one-time):

## Quick tests
1. GPU check:

2. Train test (synthetic data):
- Open Anaconda Prompt in this folder and run:

3. If you have text data:
- Put `.txt` files in `data/raw/` then run:
- Then run train with real files:

## Serve
Then POST JSON to `/generate` with `prompt_ids` (list of integers).
